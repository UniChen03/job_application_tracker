services:
  app:
    build: .
    container_name: job-application-tracker
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:8000"
    volumes:
      - job_tracker_data:/app/instance

volumes:
  job_tracker_data:
